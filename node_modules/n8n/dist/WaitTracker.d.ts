/// <reference types="node" />
import type { IExecutionsStopData } from './Interfaces';
import { ExecutionRepository } from './databases/repositories';
import { OwnershipService } from './services/ownership.service';
export declare class WaitTracker {
    private executionRepository;
    private ownershipService;
    private waitingExecutions;
    mainTimer: NodeJS.Timeout;
    constructor(executionRepository: ExecutionRepository, ownershipService: OwnershipService);
    getWaitingExecutions(): Promise<void>;
    stopExecution(executionId: string): Promise<IExecutionsStopData>;
    startExecution(executionId: string): void;
}
